# Browser-only poster fixtures

These three clearly marked images are owner-supplied artwork used only by Playwright request
interception for layout and screenshot review. They are not imported by application code, copied
to the production image, submitted to Django, or represented as real events. Synthetic API
records live in `scripts/mock-events-api.mjs` and are available only while test tooling is running.
