/**
 * Add an owner-approved ticketing hostname before publishing an external ticket URL.
 * Keeping this list empty makes the safe default explicit.
 */
export const ALLOWED_EXTERNAL_TICKET_HOSTS: readonly string[] = [];
